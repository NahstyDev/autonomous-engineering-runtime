# Autonomous Engineering Runtime — Phase 1 Dependencies
# Python 3.11+ required

# Async SQLite (Phase 2 persistence)
aiosqlite>=0.19.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

# Optional: structured logging (Phase 16)
# structlog>=24.0.0

# Optional: Postgres backend (Phase 2 alternate)
# asyncpg>=0.29.0
